
// NEW FORM

%h4 New

=simple_form_for @logo, url: {controller: 'logos', action: 'create'}, remote: true do |f|

  =f.input :name, autofocus: true, label: false, placeholder: ' logo name'
  =f.input :image_id, :as => :hidden, id: 'image_id'

  #form-gallery

  %br
  =f.button :submit, 'Create New', class: 'btn-default'

:javascript

  var ready = function () {


    // show the image gallery
      var form_name = 'logo';
      var photoset = '72157649061621222';
      var container = '#form-gallery';
      var size = 's';
      var callback = selectImage;
      var image_id_field = 'image_id';

      getFlickrImages(form_name, photoset, container, size, callback, image_id_field);

      //initialise the form buttons
      $('.btn').button();
    };

    $(document).ready(ready);
    $(document).on('page:load', ready);